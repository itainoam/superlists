server {
        listen 80;
        server_name superlists-staging.itai.com;
        location /static {
                alias /home/inoam/sites/superlists-staging.itainoam.com/static;
        }
        location / {
                proxy_set_header Host $host;
                proxy_pass http://unix:/tmp/superlists-staging.itainoam.com.socket;
        }
}